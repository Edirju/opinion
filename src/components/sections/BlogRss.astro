---
import Section from "../Section.astro";
import Rss from "../../icons/Rss.astro";
import Github from "../../icons/Github.astro";
import Linkedin from "../../icons/Linkedin.astro";
import Bluesky from "../../icons/Bluesky.astro";
import Instagram from "../../icons/Instagram.astro";
import Threads from "../../icons/Threads.astro";
import TwitterX from "../../icons/Twitter-x.astro";
const SOCIAL_ICONS: Record<string, any> = {
  Github,
  Linkedin,
  Bluesky,
  Instagram,
  Threads,
  TwitterX,
};
import { posteos } from "../../data/social-rss.json";
---

<Section>
  <article
    class="flex flex-col gap-6 font-[family-name:var(--body)] text-white/65 animate-fade-in-up"
  >
    <header
      class="flex flex-col gap-2 text-orange-400/85 font-[family-name:var(--body)]"
    >
      <h2
        class="flex items-center gap-1 capitalize font-bold text-pretty tracking-tight text-[clamp(1.125rem,1.0909rem_+_0.1818vw,1.25rem)]"
      >
        <Rss /><span class="font-extralight text-orange-400/45">|</span> Social Media:
      </h2>
      <p
        class="text-sm font-extralight text-pretty tracking-tight text-white/65 [&>strong]:text-orange-400/65 [&>strong]:font-medium"
      >
        Este es el feed del pensamiento en voz alta. Notas rápidas, enlaces que
        me volaron la cabeza y esas ideas que no pueden esperar. <strong
          >Directo, breve y al grano.</strong
        >
      </p>
    </header>
    <footer>
      <ul class="flex flex-col gap-2">
        {
          posteos.map(({ network, title, url, date }) => {
            const Icon = SOCIAL_ICONS[network];
            if (!Icon) return null;

            return (
              <li>
                <a
                  href={url}
                  title={`Click para leer más: ${title}`}
                  class="flex items-center gap-2"
                >
                  <figure class="border border-[rgb(var(--primary),0.65)] p-2 rounded-full">
                    <Icon />
                  </figure>
                  <h3 class="text-[clamp(0.875rem,0.838rem_+_0.1852vw,1rem)] font-extralight tracking-tight capitalize text-pretty p-0 m-0 relative transition-all duration-300 ease-in-out after:content-[''] after:absolute after:left-0 after:right-0 after:top-full after:h-0.5 after:bg-[rgb(var(--primary),0.65)] after:transform-[scaleX(0)] after:transition-transform after:origin-left hover:text-[rgb(var(--primary),0.65)] hover:after:transform-[scaleX(1)]">
                    {title}
                  </h3>
                  <time class="text-[clamp(0.6875rem,0.6505rem_+_0.1852vw,0.8125rem)] font-extralight text-[rgb(var(--primary),0.5)]">
                    {date}
                  </time>
                </a>
              </li>
            );
          })
        }
      </ul>
    </footer>
  </article>
</Section>

<style>  
  @plugin "@midudev/tailwind-animations";
</style>
